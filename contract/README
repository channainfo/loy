# Getting started

## Sui CLI

```sh
sui client

> Generated new keypair and alias for address with scheme "ed25519" [musing-carnelian: 0xb8e9c7271d32e3a60c3f008be3b51e941c38dad4c722299c8f4846ae002d72d9]
> Secret Recovery Phrase : [resist inject angry tortoise tone real section define turkey utility truck awake]
?Client for interacting with the Sui network
```

### Sui networks

#### list of networks

```sh
sui client envs

> https://fullnode.testnet.sui.io:443

```

Output
│ alias   │ url                                   │ active │
├─────────┼──────────────────────────────────--───┼────────┤
│ testnet │ <https://fullnode.testnet.sui.io:443> │ *      │

- Testnet: <https://fullnode.testnet.sui.io:443>
- Devenet: <https://fullnode.devnet.sui.io:443>

```sh
# sui client new-env --alias <ALIAS> --rpc <RPC-SERVER-URL>
sui client new-env --alias devnet --rpc https://fullnode.devnet.sui.io:443
```

sui client envs

╭─────────┬─────────────────────────────--────────┬────────╮
│ alias   │ url                                   │ active │
├─────────┼─────────────────────────────--────────┼────────┤
│ testnet │ <https://fullnode.testnet.sui.io:443> │ *      │
│ devnet  │ <https://fullnode.devnet.sui.io:443>  │        │
╰─────────┴────────────────────────────────────--─┴────────╯

#### Switch network

```sh
sui client switch --env devnet
```

#### Active networks

```
sui client active-env
```

### Sui Address

```sh
sui client new-address ed25519
```

output

│ Created new keypair and saved it to keystore.                                                   │
├────────────────┬────────────────────────────────────────────────────────────────────────────────┤
│ alias          │ naughty-hiddenite                                                              │
│ address        │ 0x778bcc16e6aaa7eb69f25afb6e12a0d2682f29871017cc70723b282c5cb9eebd             │
│ keyScheme      │ ed25519                                                                        │
│ recoveryPhrase │ riot sausage defense exhibit vocal tail dignity plunge bulb blouse great scrub

#### List all the local address

```
sui keytool list
```

│ alias           │  naughty-hiddenite                                                   │
│ suiAddress      │  0x778bcc16e6aaa7eb69f25afb6e12a0d2682f29871017cc70723b282c5cb9eebd  │
│ publicBase64Key │  AKcvnTH79VNbZ+kDCTIVzH/wgdvKMDDVU2KubiRCJoyb                        │
│ keyScheme       │  ed25519                                                             │
│ flag            │  0                                                                   │
│ peerId          │  a72f9d31fbf5535b67e903093215cc7ff081dbca3030d55362ae6e2442268c9b    | 

or you can use

```sh
sui client addresses
```

#### Switch address

```sh
sui client switch --address naughty-hiddenite
# Active address switched to 0x778bcc16e6aaa7eb69f25afb6e12a0d2682f29871017cc70723b282c5cb9eebd
```

#### Active address

```sh
sui client active-address
# 0x778bcc16e6aaa7eb69f25afb6e12a0d2682f29871017cc70723b282c5cb9eebd
```

#### Local private keys

Your sui local private keys are stored in ~/.sui/sui_config/sui.keystore.

```
cat ~/.sui/sui_config/sui.keystore
```


### Getting some sui token

check you active-env first to ensure you are on the right network

#### Request sui token

```
sui client faucet
# Request successful. It can take up to 1 minute to get the coin. Run sui client gas to check your gas coins.
```

#### Check sui token balance

Sui token balance ( gas )
╭────────────────────────────────────────────────────────────────────┬────────────────────┬──────────────────╮
│ gasCoinId                                                          │ mistBalance (MIST) │ suiBalance (SUI) │
├────────────────────────────────────────────────────────────────────┼────────────────────┼──────────────────┤
│ 0xc7b33b8bed15849918d04b24439f6b7433d2c7a33eda3cd4b139ba89122b0039 │ 10000000000        │ 10.00            │
╰────────────────────────────────────────────────────────────────────┴────────────────────┴──────────────────╯
